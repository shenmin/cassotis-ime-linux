# 配置说明

[English](CONFIGURATION.md) | 简体中文

言泉输入法的设置由当前用户的共享引擎统一保存，因此 IBus 与 Fcitx 5 使用
相同的状态和用户词库。可以从桌面应用列表或输入法框架的 Cassotis 属性菜单
打开 **言泉输入法 - 设置**。

## 常规

| 配置项 | 可选值 | 默认值 | 说明 |
| --- | --- | --- | --- |
| 输入模式 | 简体中文、繁体中文、英文 | 简体中文 | 同时选择中英文状态及当前基础词库。 |
| 拼音方案 | 全拼；微软、小鹤、自然码、搜狗、紫光、拼音加加双拼 | 全拼 | 只改变键盘编码；所有方案共用召回、排序、学习和补全逻辑。 |
| 标点 | 中文标点、英文标点 | 中文标点 | 选择中文全角标点映射或英文原字符。 |
| 字符宽度 | 半角、全角 | 半角 | 启用全角时转换可打印 ASCII 字符。 |

切换输入模式、拼音方案或模糊拼音规则时会清除尚未上屏的组合，避免已有按键
被另一套规则重新解释。

## 候选

每页候选数可设为 3 至 9，默认是 9。Linux 下候选面板的字体、颜色、位置、
缩放和排列方向由 IBus/Fcitx 及桌面主题负责；言泉输入法不自行绘制或定位
独立候选浮窗。

## 受控模糊拼音

模糊拼音默认关闭。可以分别启用 `z/zh`、`c/ch`、`s/sh`、`l/n`、`f/h`、
`r/l`、`an/ang`、`en/eng`、`in/ing`、`ian/iang`、`uan/uang`。

引擎只对不超过四个音节的完整输入应用至多一次已配置变化；原始精确拼写
始终优先于模糊匹配。前缀、简拼和任意长句路径不会进行模糊扩展。

## 翻页与一键补全

候选翻页可选 `-/+`、`[/]`、`,/.` 或 `Shift+Tab/Tab`。一键补全可选 `Tab`
或反引号键。`Tab` 不能同时用于翻页和补全，设置程序会在应用前解决冲突。

输入至少两个完整音节，并且词库或用户转移信号能够从精确前缀继续时，才会
显示一键补全。补全与普通候选分开显示，不会改变普通候选顺序。

## 功能快捷键

| 功能 | 默认快捷键 |
| --- | --- |
| 中英文切换 | `Shift` |
| 中英文标点切换 | `Ctrl+.` |
| 简繁词库切换 | `Ctrl+Shift+T` |
| 全半角切换 | `Shift+Space` |
| 打开设置 | `Ctrl+Shift+F10` |

快捷键不可重复。没有 Ctrl 或 Alt 修饰键时，只允许使用 Shift 或 F1-F24，
避免意外占用普通文字输入。

## 用户数据与诊断

“高级”页可以在候选注释中显示引擎分数、打开数据目录，或在确认后清空用户
词库。清空会删除已学习词和相关偏好，且无法撤销。

默认路径遵循 XDG 基础目录规范：

```text
~/.local/share/cassotis-ime/user_dict.db
~/.local/share/cassotis-ime/dict_sc.db
~/.local/share/cassotis-ime/dict_tc.db
$XDG_RUNTIME_DIR/cassotis-ime/engine.sock
```

系统安装包把只读基础词库放在 `/usr/share/cassotis-ime/`；上述用户目录中
存在词库时优先使用用户版本。软件包和便携包卸载都不会删除学习数据。
