# 质量与一致性验收

[English](BENCHMARK.md) | 简体中文

发行门禁由源码与数据的机械一致性检查、Linux 原生单元测试与集成测试、
有边界的传输和内存压力测试、冻结候选一致性测试，以及语料规模的质量与
延迟基准测试共同组成。任何单项指标都不能单独证明两个平台完全一致。

## 共同语料来源

Linux 的语料规模质量门禁使用与
[言泉输入法 Windows 版](https://github.com/shenmin/cassotis-ime/blob/master/BENCHMARK.CN.md)
相同的固定长句和短词基准案例。两套基准的语料均来自开发者自己的小说著作
[**《永恒的舞动》**](https://www.qidian.com/book/1037259117/)
（英文名 **Elegance in Timelessness**）。基准案例与对应模型的训练数据保持隔离。

## 基准版本

当前 Linux 引擎依据以下版本完成审查和移植：

- Cassotis IME v1.21.0（`a06df4c9150ac4fcd140b8709c53c5b7bf7e1be4`）
- Cassotis Lexicon v1.21.0（`63f4df366f3b62d4ebad2e3192811d5d1e4e3f2b`）
- 简体词库 schema 24，SHA-256：
  `fc6800d88d67d3b68b6ccb1b9f1832cd5f5a17598c5b28a9109f9da12985ee37`
- 繁体词库 schema 24，SHA-256：
  `845d7c63de2d03ba6bacac66c699b99c5256afe58332eacb2326ca42a9682681`
- 简体/繁体基础词条：213,233 / 216,385
- 简体/繁体补全竞争信号：42,453 / 42,448 条
- 简体/繁体补全 pair-audit 信号：4,379 / 4,379 条
- 简体长句补全表：35,423 条可见路径、97,589 条文本召回记录
- 繁体长句补全表：32,481 条可见路径、92,457 条文本召回记录

`tools/parity/validate_source_parity.py` 检查两个基准仓库的提交，分别
冻结并校验两端已经审阅的生产引擎、SQLite provider、拼音解析器、模糊音和
双拼源码清单，同时校验全部 42 个生成模型单元、扩展模型证据和冻结词库。
清单还会绑定 Transformer 评分器、受约束拼音与补全生成器、运行时允许表、
索引和清单、原生推理桥、三份经审阅的原生补全选择器源码、各架构对应的
ONNX Runtime，以及基础词条、补全竞争、
pair-audit 与长句补全表
的必需行数。清单独立绑定
Delphi 与 FPC 的平台适配源码，并不伪称两端文件可以逐字相同。
随后通过实际 SQLite provider 运行小型回归集 `tests/cases/candidate_quality.tsv` 和
`tests/cases/candidate_quality_tc.tsv`，保护已确认的简体与繁体候选行为。
完整质量门禁还会在排除依赖主机的耗时后计算规范化失败签名。确定性的短词轨道
要求逐样本签名完全一致；神经长句轨道必须达到 Windows 公布的汇总名次下限。
完整基准之前，另用 500 条样本在三组独立进程中重复运行确定性候选与补全检查，
刻意改变命令行参数及环境布局。记录的候选、路径或补全决策发生任何差异都会
使该检查失败，不把未初始化搜索状态视为可以接受的运行时波动。长句失败 TSV
继续保留完整诊断信息，用于分析跨平台差异，不用汇总计数冒充逐条完全一致。

原生运行时还加入了量化推理的精确整数运算回归检查。四个模型会话统一启用
ONNX Runtime 的 x86 量化精度模式，避免不支持 VNNI 的 CPU 出现中间乘积
饱和；模型文件和量化尺度保持不变。

公开的语料对比会关闭持久化用户学习和外部文档上下文，与 Windows 基准协议
保持一致。文档局部自适应由独立的模型与服务测试验证，避免一个应用上下文中的
文本静默影响冻结的汇总成绩。

## Linux 完整基准测试

`cassotis-quality-benchmark` 在 Linux 上原生运行，使用与 Windows 项目
相同的最终词库和冻结测试集：

- 16,300 条长句测试
- 65,000 条无上下文短词测试
- 同一组 65,000 条带冻结左侧上下文的短词测试

直接构建并运行：

```bash
./scripts/build.sh
./build/bin/cassotis-quality-benchmark \
  --dictionary /path/to/dict_sc.db \
  --long-cases /path/to/long_sentence_16300.tsv \
  --short-cases /path/to/word_input_yhwd_context.tsv \
  --neural-runtime ./build/bin \
  --report-dir ./quality-report

python3 tools/parity/validate_quality_report.py \
  --summary ./quality-report/quality-summary.txt \
  --dictionary /path/to/dict_sc.db \
  --long-cases /path/to/long_sentence_16300.tsv \
  --short-cases /path/to/word_input_yhwd_context.tsv \
  --baseline tests/baselines/quality-v1.21.0-linux-x86_64.txt
```

长句准确率轨道使用确定性工作量上限和单线程 ONNX 推理，不以墙钟时间截断
已经完成的 Transformer 决策；独立的生产模式轨道使用实际部署的模型并行度和
30 ms 诊断阈值测量延迟。v1.21 中，同步推理即使超过该阈值，已经完成的结果
仍可参与排序；该阈值不是取消截止时间。两个轨道使用同一模型和有界搜索，
分离测试可减少并行度造成的准确率波动，同时保留真实生产延迟。上述独立进程
复测负责检查同平台稳定性，不预设不同 CPU 架构的神经决策逐位一致。
独立的异步补全基准仍保留实际部署的 40 ms
结果接受截止时间。

测试程序报告 Top1、Top2、Top5、Top9 数量，平均、P50、P95 和最大查询
延迟，以及 Linux 进程 RSS 与内存高水位。内存事件只记录测试轨道和样例
编号。所有非 Top1 结果都会写入 `long-failures.tsv` 或
`short-failures.tsv`；这些文件仅用于本地诊断，并不代表测试被忽略，
也不会包含在二进制发行制品中。

## 冻结的 v1.21.0 移植结果

v1.21.0 移植继续使用与 v1.20.0 相同、单独提供的 16,300 条长句和
65,000 条短词测试，输入字节数及 SHA-256 见下一节。以下为 2026-09-05 在
Ubuntu 26.04.1 GNOME Wayland 双架构主机上的验证测量。发行包还必须针对其
精确源码提交重新通过完整门禁；这些测量不替代每个版本独立的发行验收记录。

| 架构 | 长句 Top1 | 长句 Top2 / Top5 / Top9 | 平均 | P50 | P95 | 最大 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| x86_64 | 11,080/16,300 | 12,396/16,300 | 150.750 ms | 158 ms | 275 ms | 913 ms |
| aarch64 | 11,088/16,300 | 12,412/16,300 | 76.522 ms | 72 ms | 141 ms | 574 ms |

Windows v1.21.0 公布值为 Top1 11,080、Top2 12,395。Linux x86_64 的 Top1
相同、Top2 高 1 条，aarch64 分别高 8/17 条。两个架构的发行下限都要求至少达到
Windows 公布值，没有通过设置更低的平台准确率目标放行。这是汇总质量对齐，
不是逐条完全相同：与 Windows 精确标签重跑相比，x86_64 有 2 条 Top1
转对、2 条转错，共 7 条目标名次不同；aarch64 有 50 条转对、42 条转错，
共 114 条目标名次不同。尚未确定每个神经结果差异的成因。
延迟依赖主机性能，不能直接解释成跨平台实现速度的倍数。

完整短词轨道在两个 Linux 架构与 Windows 上的计数一致：

| 轨道 | Top1 / 65,000 | Top2 / 65,000 | Top5 | Top9 | 竞争集 Top1 / 11,728 | 竞争集 Top2 / 11,728 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 无上下文 | 60,346 | 63,163 | 64,499 | 64,620 | 8,737 | 10,528 |
| 有上下文 | 61,827 | 63,517 | 64,541 | 64,620 | 9,596 | 10,775 |

短词精确失败签名为 7,827 行，SHA-256 为
`18cad226349cbfd1451c35c25f9572b3e004f121c79c5c22658fe47b970b207b`。
长句进程的 RSS/内存高水位在 x86_64 为 825,004 KiB，在 aarch64 为
810,712 KiB，均低于已有的 1,048,576 KiB 门禁。两个架构均通过 146 个
FPCUnit 测试、22 条简体和 9 条繁体候选回归，以及五阶段自动化 IBus/Fcitx
平台矩阵。

短词一键补全新增原生基准，使用与 Windows 相同的 12,831 个增量前缀机会点
及冻结的左侧上下文：

| 架构 | 补全命中 | 平均节省按键 | 稳定性 | P95 |
| --- | ---: | ---: | ---: | ---: |
| x86_64 | 9,420/12,831 | 2.548 | 1,691/1,749 | 3 ms |
| aarch64 | 9,420/12,831 | 2.548 | 1,691/1,749 | 2 ms |

两个架构均节省 24,006 键，可见决策的精确签名均为 `D33AC07C1551CAA1`，
与 Windows v1.21.0 精确标签重跑一致。Windows README 已公布的历史行是
9,419 次命中、平均节省 2.549 键，不能将多出 1 次命中的重跑值冒充原公布值。

独立的 16,300 条生产长句补全轨道继续保留真实的 40 ms 异步结果接受窗口：

| 架构 | 局部补全命中 | 提示覆盖 | 节省按键总数 | P95 |
| --- | ---: | ---: | ---: | ---: |
| x86_64 | 280/16,300 | 5,249/16,300 | 703 | 69 ms |
| aarch64 | 362/16,300 | 6,478/16,300 | 853 | 45 ms |

Windows 公布值为 357 次命中、6,475 次提示、节省 861 键。与确定性的短词
补全不同，带截止时间的后台轨道在验证主机上没有达到相同计数。超过部署版
接受窗口的结果仍然拒绝，不放宽截止时间来制造表面一致。500 条关闭截止时间
的 smoke 按架构要求精确签名：x86_64 为 `06E92EB69EB24518`，aarch64 为
`4775EE37F80823C9`。

x86_64 的全部现有补全约束均通过。aarch64 接受并应用了 3,209 次神经补全，
超过这两个计数已有的 3,200 次上限；其余补全约束均通过。这两项上限保持不变，
等待审查，不能把这轮验证报告成完整发行门禁通过。aarch64 补全进程的最大 RSS
为 809,832 KiB。另一个新进程重跑前 5,000 条完整长句，首选文本和目标名次均
与全量结果逐条一致。

## 冻结的 v1.20.0 移植结果

v1.20.0 验收继续使用与 v1.19.0 相同的外部冻结语料。本仓库不重新分发这些
语料文件，并继续以字节数和 SHA-256 绑定输入：

| 输入 | 样例数 | 字节数 | SHA-256 |
| --- | ---: | ---: | --- |
| 长句 | 16,300 | 2,673,936 | `3f50a9323ad798e691f86ea70c6dffa13b4a9f55b624fc3499a138258190ff0f` |
| 带冻结上下文的短词 | 65,000 | 9,200,779 | `cd02fc1a24e89a106c200f4864d5ad2c11afd4c8d784059a4b6e9a10c51fbab8` |

Ubuntu 26.04.1 x86_64 与 Ubuntu 26.04.1 aarch64 使用经过审查的 v1.20.0
引擎、schema 24 词库、文档局部自适应、条件 Transformer 与受约束候选生成器，
得到以下原生结果。语料对比关闭外部文档上下文；准确率轨道为确定性运行，
延迟轨道采用实际部署的 30 ms 神经结果接受预算：

| 架构 | 轨道 | Top1 | Top2 | Top5 | Top9 | 平均 | P50 | P95 | 最大 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| x86_64 | 长句 | 11,088/16,300 | 12,402/16,300 | 12,402 | 12,402 | 184.969 ms | 180 ms | 363 ms | 936 ms |
| x86_64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 8.967 ms | 7 ms | 23 ms | 68 ms |
| x86_64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 10.059 ms | 8 ms | 25 ms | 66 ms |
| aarch64 | 长句 | 11,068/16,300 | 12,393/16,300 | 12,393 | 12,393 | 104.667 ms | 91 ms | 221 ms | 612 ms |
| aarch64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 5.957 ms | 5 ms | 15 ms | 44 ms |
| aarch64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 6.617 ms | 5 ms | 16 ms | 45 ms |

Windows v1.20.0 公布参考值为 Top1 11,065、Top2 12,376。Linux x86_64
分别高 23/26 条，aarch64 分别高 3/17 条。与冻结的 Linux v1.19.0 结果相比，
v1.20.0 在 x86_64 上增加 147 条 Top1、142 条 Top2，在 aarch64 上增加
152 条 Top1、141 条 Top2。65,000 条短词在 Windows、x86_64 与 aarch64
上的汇总计数和精确逐样本失败签名保持完全一致。

质量基准的最大 RSS/内存高水位在 x86_64 上为 843,860/865,160 KiB，
在 aarch64 上为 936,084/967,068 KiB；完整补全进程分别峰值为
976,620 KiB 和 946,128 KiB，均低于 983,040 KiB 发布上限。两次干净原生
构建均通过全部 141 个 FPCUnit 测试、22 个简体与 9 个繁体候选回归，以及
八上下文、8,300 键的传输与重启压力测试。

完整一键补全轨道继续使用生产环境的 40 ms 本地结果接受上限：

| 架构 | 本地补全命中 | 提示覆盖 | 总节省按键 | P95 |
| --- | ---: | ---: | ---: | ---: |
| x86_64 | 303/16,300（1.86%） | 5,510/16,300（33.80%） | 724 | 141 ms |
| aarch64 | 349/16,300（2.14%） | 6,283/16,300（38.55%） | 829 | 99 ms |

Windows v1.20.0 公布值为 357 次命中、6,475 次提示、节省 861 键，P95 为
42.672 ms。较慢的 x86_64 验证主机更容易跨过固定的 40 ms 后台结果边界，
因此接受的提示与命中少于 aarch64；生成器、置信策略和用户可见超时均未改变。
门禁记录这一真实生产行为，而不会通过放宽超时制造表面一致。

独立的 500 条确定性神经 smoke 会关闭墙钟截止，并分别冻结 x86_64 的
`A53F2BCDD29A210C` 与 aarch64 的 `BB58A43A76877A5B`。与 v1.19.0 不同，
v1.20.0 会实际执行生成式 ONNX 路径，其 beam 决策可能在不同架构上跨过浮点
边界。因此两个架构各自冻结精确 smoke 签名，同时继续共享同一模型、允许输出表、
结果校验策略和汇总发布下限。

## 冻结的 v1.19.0 移植结果

v1.19.0 验收继续使用与 v1.18.0 相同的外部冻结语料。本仓库不重新分发这些
语料文件，并继续以字节数和 SHA-256 绑定输入：

| 输入 | 样例数 | 字节数 | SHA-256 |
| --- | ---: | ---: | --- |
| 长句 | 16,300 | 2,673,936 | `3f50a9323ad798e691f86ea70c6dffa13b4a9f55b624fc3499a138258190ff0f` |
| 带冻结上下文的短词 | 65,000 | 9,200,779 | `cd02fc1a24e89a106c200f4864d5ad2c11afd4c8d784059a4b6e9a10c51fbab8` |

Ubuntu 26.04.1 x86_64 与 Ubuntu 26.04.1 aarch64 使用经过审查的 v1.19.0
引擎、schema 24 词库、条件 Transformer、学习式门控/融合参数和补全索引 v2，
得到以下原生结果。与上一版门禁相同，准确率轨道为确定性运行，延迟轨道采用
实际部署的 30 ms 神经结果接受预算：

| 架构 | 轨道 | Top1 | Top2 | Top5 | Top9 | 平均 | P50 | P95 | 最大 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| x86_64 | 长句 | 10,941/16,300 | 12,260/16,300 | 12,260 | 12,260 | 183.438 ms | 175 ms | 364 ms | 1,023 ms |
| x86_64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 9.248 ms | 7 ms | 24 ms | 89 ms |
| x86_64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 10.365 ms | 8 ms | 26 ms | 81 ms |
| aarch64 | 长句 | 10,916/16,300 | 12,252/16,300 | 12,252 | 12,252 | 103.540 ms | 89 ms | 219 ms | 664 ms |
| aarch64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 5.933 ms | 5 ms | 14 ms | 47 ms |
| aarch64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 6.592 ms | 5 ms | 16 ms | 47 ms |

Windows v1.19.0 公布参考值为 Top1 10,917、Top2 12,248。Linux x86_64
分别高 24/12 条，aarch64 分别低 1 条和高 4 条，因此两个架构均达到 Windows
汇总质量目标。少量逐样本差异来自 ONNX Runtime 与浮点计算的模型决策边界，
并非架构专用排序策略。65,000 条短词在 Windows、x86_64 和 aarch64 上的
汇总计数与精确逐样本失败签名完全一致。

与冻结的 Linux v1.18.0 结果相比，v1.19.0 在 x86_64 上增加 254 条 Top1、
156 条 Top2，在 aarch64 上增加 212 条 Top1、145 条 Top2。两个架构的峰值
RSS/内存高水位分别为 850,120 KiB 与 869,564 KiB，均低于 960 MiB 门禁；
两次干净原生构建均通过全部 132 个 FPCUnit 测试。延迟依赖测试主机，不能把
不同硬件上的数据直接解释成实现速度倍数。

完整的一键补全测试采用与 Windows 相同的“省略末尾四个音节”口径，并同时统计
静态补全与神经补全：

```bash
./build/bin/cassotis-completion-benchmark \
  /path/to/dict_sc.db /path/to/long_sentence_16300.tsv 16300 40 500
```

第四个参数使用与生产 Host 和 Windows 基准一致的 40 ms 本地结果接受上限；
第五个参数每 500 条输出一次进度。测试程序还会在计时样本之后执行与 Windows
相同的只读 oracle 检查，使缓存预热顺序可比，但不改变已经测得的生产结果。

| 架构 | 本地补全命中 | 提示覆盖 | 总节省按键 | P95 |
| --- | ---: | ---: | ---: | ---: |
| x86_64 | 194/16,300（1.19%） | 3,719/16,300（22.82%） | 556 | 140 ms |
| aarch64 | 191/16,300（1.17%） | 3,726/16,300（22.86%） | 548 | 95 ms |

Windows v1.19.0 公布值为 202 次命中、3,834 次提示、节省 571 键，P95 为
38.452 ms。关闭墙钟上限后，Linux 得到与两个架构完全一致的确定性补全决策
签名；生产轨道中则有更多后台结果越过 40 ms 边界而不被接受。因此门禁保持
真实生产超时并记录实测差异，而不是通过放宽超时制造相同计数。

完整生产轨道只冻结质量与延迟区间，不冻结补全签名。主机调度可能使后台结果
跨过 40 ms 边界，进而改变后续样本看到的缓存状态。独立的 500 条确定性
`cassotis-neural-engine-smoke` 会关闭两处神经墙钟上限，只隔离神经回退层
生命周期，并要求 x86_64 与 aarch64 均得到完全相同的
`DCB3F73CD5277B97` 签名。

## 冻结的 v1.18.0 移植结果

完整发行测试使用单独提供的冻结语料文件。小说原文不属于软件发行内容，
因此本仓库不重新分发这些文件。发行记录通过文件大小和 SHA-256 绑定输入，
避免将另一组测试集的结果静默复用：

| 输入 | 样例数 | 字节数 | SHA-256 |
| --- | ---: | ---: | --- |
| 长句 | 16,300 | 2,673,936 | `3f50a9323ad798e691f86ea70c6dffa13b4a9f55b624fc3499a138258190ff0f` |
| 带冻结上下文的短词 | 65,000 | 9,200,779 | `cd02fc1a24e89a106c200f4864d5ad2c11afd4c8d784059a4b6e9a10c51fbab8` |

Ubuntu 26.04 x86_64 与 Ubuntu 26.04.1 aarch64 发行主机使用经过审查的
schema 24 简体词库，得到以下原生引擎结果。候选名次来自不设置神经模型
墙钟超时的确定性准确率测试；耗时列来自另一轮采用部署版 30 ms 神经结果
预算的生产模式测试：

| 架构 | 测试轨道 | Top1 | Top2 | Top5 | Top9 | 平均 | P50 | P95 | 最大值 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| x86_64 | 长句 | 10,687/16,300 | 12,104/16,300 | 12,104 | 12,104 | 149.270 ms | 134 ms | 305 ms | 1,024 ms |
| x86_64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 9.038 ms | 7 ms | 23 ms | 67 ms |
| x86_64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 10.120 ms | 8 ms | 25 ms | 87 ms |
| aarch64 | 长句 | 10,704/16,300 | 12,107/16,300 | 12,107 | 12,107 | 86.083 ms | 72 ms | 188 ms | 566 ms |
| aarch64 | 短词，无上下文 | 60,346/65,000 | 63,163/65,000 | 64,498 | 64,619 | 5.394 ms | 4 ms | 13 ms | 42 ms |
| aarch64 | 短词，有上下文 | 61,827/65,000 | 63,517/65,000 | 64,540 | 64,619 | 5.986 ms | 5 ms | 14 ms | 41 ms |

11,728 条标记为真实竞争候选的短词样例，在两个架构上均于无上下文时取得
8,737/10,528 的 Top1/Top2，在有上下文时取得 9,596/10,775。x86_64 与
aarch64 的全部短词汇总计数和逐样本失败签名完全一致。Windows v1.18.0
长句参考值为 Top1 10,731、Top2 12,128。Linux 长句结果与之接近，但 ONNX
Runtime 和浮点计算可能使少量样本跨过模型决策边界，因此两个架构分别冻结
精确逐样本签名；生产排序逻辑中不存在架构专用分支。延迟依赖测试主机，不能
把不同硬件上的数据直接解释成实现速度倍数。

v0.2.0 将字符语言模型的 span 评分改为不依赖先前无关查询留下的 n-gram
缓存。该项经过审查的运行时行为变更需要重新冻结长句签名。与临时基线相比，
x86_64 有 23 条 Top1 由错转对、21 条由对转错，净增 2 条；aarch64 有
3 条转对、2 条转错，净增 1 条。汇总质量下限没有降低，短词逐样本签名也
没有变化。

x86_64 与 aarch64 基准进程的最大 RSS/内存高水位分别为 763,640 KiB 和
866,268 KiB，均低于 960 MiB 的发行上限。两次干净构建门禁均通过全部
129 个 FPCUnit 测试、22/22 个简体冻结候选、9/9 个繁体冻结候选，以及
确定性的 500 样本神经补全测试。八上下文、8,300 次按键的传输测试在
x86_64 上平均为 19,472.237 微秒、最大为 129,987 微秒，预热后 RSS 增长
8 KiB；在 aarch64 上平均为 11,604.921 微秒、最大为 41,059 微秒，预热后
RSS 无增长。两个架构均通过引擎重启恢复测试。

## 完整发行门禁

首先在同时具有 Windows 和词库仓库 checkout 的机器上生成源码一致性报告：

```bash
python3 tools/parity/validate_source_parity.py \
  --windows-root /path/to/cassotis-ime \
  --lexicon-root /path/to/cassotis-lexicon \
  --dictionary /path/to/dict_sc.db \
  --dictionary-traditional /path/to/dict_tc.db \
  --report source-parity.json
```

然后在目标 Linux 桌面会话中运行发行门禁：

```bash
./scripts/validate_release.sh \
  --dictionary /path/to/dict_sc.db \
  --dictionary-traditional /path/to/dict_tc.db \
  --long-cases /path/to/long_sentence_16300.tsv \
  --short-cases /path/to/word_input_yhwd_context.tsv \
  --source-parity-report /path/to/source-parity.json \
  --report-dir ./release-validation
```

生成的 `release-validation.json`、平台矩阵、日志、基准文件、安装包校验和
与安装包共同构成一份可审计的发行记录。

仓库中的 x86_64 与 aarch64 基线是最低发行门槛，不是训练目标。它们要求
测试样例数量完整，平均、P95、最大延迟和峰值内存有明确上限，并冻结短词逐样本
失败签名和确定性 500 条神经补全签名，同时约束神经长句汇总名次及完整语料补全
的质量与延迟。只有更换冻结语料、经过审查的引擎基线或明确记录的运行时变更时
才能更新对应文件，不能通过降低阈值掩盖质量退化。

## 如何理解结果

本基准测量同步引擎查询时间，不包含按键投递、候选窗口绘制、桌面合成器
延迟或网络推理。测试有意不使用持久化用户词库；实际用户学习可以改善
个人候选排序，其行为由服务层和适配器测试单独验证。
